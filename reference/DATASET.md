# Two-stage example Small example dataset for demonstrating matching, censoring imputation, and treatment-rule learning functions in DRMLSurv.

Two-stage example Small example dataset for demonstrating matching,
censoring imputation, and treatment-rule learning functions in DRMLSurv.

## Usage

``` r
data(DATASET)
```

## Format

An object of class `Drmatch`

## Arguments

- DATASET:

  The data for moderation analysis

## Examples

``` r
head(data(DATASET))
#> [1] "DATASET"
```
