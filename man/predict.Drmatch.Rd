% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarizer.R
\name{predict.Drmatch}
\alias{predict.Drmatch}
\title{Predict optimal treatment decisions from a fitted Drmatch object}
\usage{
\method{predict}{Drmatch}(object, newdata, stage = c("both", "stage1", "stage2"), ...)
}
\arguments{
\item{object}{A fitted \code{Drmatch} object containing the trained
stage-specific \pkg{ranger} random forest models and metadata needed for
deployment, including \code{stage1_model}, \code{stage2_model}, \code{names.var1},
\code{names.var2}, and \code{eta2.var}.}

\item{newdata}{A data frame containing the predictor variables required for
stage-1 and/or stage-2 prediction.}

\item{stage}{Character string indicating which stage predictions to return.
Must be one of \code{"both"}, \code{"stage1"}, or \code{"stage2"}. The default is
\code{"both"}.}

\item{...}{Additional arguments passed through for S3 compatibility.}
}
\value{
A data frame with one row per row of \code{newdata}. The returned data frame
always contains:

\describe{
\item{\code{row_id}}{Row index corresponding to the original row position in \code{newdata}.}
}

Depending on \code{stage}, it also contains:

\describe{
\item{\code{A1.opt}}{Predicted optimal stage-1 treatment, or \code{NA} when stage-1 prediction is not available for that row.}
\item{\code{A2.opt}}{Predicted optimal stage-2 treatment, or \code{NA} when stage-2 prediction is not available for that row.}
}
}
\description{
Generates predicted optimal treatment assignments from a fitted
\code{Drmatch} object for new observations using stage-specific random
forest models fitted with the \pkg{ranger} package. Predictions may be
obtained for stage 1 only, stage 2 only, or both stages of a two-stage
treatment regime.
}
\details{
For stage 1, predictions are produced for rows in \code{newdata} with complete
values on the stage-1 predictor set stored in \code{object$names.var1}. For
stage 2, predictions are produced only for rows that are eligible for
second-stage treatment, as indicated by \code{object$eta2.var == 1}, and that
also have complete values on the stage-2 predictor set stored in
\code{object$names.var2}.

Rows that are ineligible for prediction because of missing required
covariates, or because they are not eligible for stage 2, receive \code{NA}
for the corresponding predicted treatment.

This function is an S3 \code{predict()} method for objects of class
\code{Drmatch}.

When \code{stage = "stage1"}, the function checks that all variables listed in
\code{object$names.var1} are present in \code{newdata}. Predictions are returned in
the column \code{A1.opt}.

When \code{stage = "stage2"}, the function checks that all variables listed in
\code{object$names.var2} are present in \code{newdata}, and also verifies that the
stage-2 eligibility indicator named by \code{object$eta2.var} is available in
\code{newdata}. Predictions are returned in the column \code{A2.opt}.

When \code{stage = "both"}, both \code{A1.opt} and \code{A2.opt} are returned.

Internally, stage-specific predictions are obtained from fitted
\pkg{ranger} models using the \code{predictions} component returned by
\code{\link[=predict.ranger]{predict.ranger()}}:
\preformatted{
predict(model, data = newdata_subset)$predictions
}
For classification forests, these predicted class labels are coerced to
numeric values, so the fitted models are expected to predict treatment
classes coded as \code{-1} and \code{1}.
}
\section{Required components of \code{object}}{

The fitted \code{Drmatch} object is expected to contain at least the
following elements:
\describe{
\item{\code{stage1_model}}{A fitted \pkg{ranger} classification model for stage 1.}
\item{\code{stage2_model}}{A fitted \pkg{ranger} classification model for stage 2.}
\item{\code{names.var1}}{Character vector of predictor names required for stage 1.}
\item{\code{names.var2}}{Character vector of predictor names required for stage 2.}
\item{\code{eta2.var}}{Name of the stage-2 eligibility indicator in \code{newdata}.}
}
}

\examples{
## Not run:
## Suppose `fit` is a fitted Drmatch object
## and `new_patients` is a data frame of candidate patients.
##
## Predict both stages
## pred <- predict(fit, newdata = new_patients, stage = "both")
##
## Predict stage 1 only
## pred1 <- predict(fit, newdata = new_patients, stage = "stage1")
##
## Predict stage 2 only
## pred2 <- predict(fit, newdata = new_patients, stage = "stage2")

}
