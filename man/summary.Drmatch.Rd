% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarizer.R
\name{summary.Drmatch}
\alias{summary.Drmatch}
\title{Summarize a fitted \code{Drmatch} object on new data}
\usage{
\method{summary}{Drmatch}(
  object,
  newdata,
  stage = c("both", "stage1", "stage2"),
  obs1_var = NULL,
  obs2_var = NULL,
  eta2_var = NULL,
  pred1_var = "A1.opt",
  pred2_var = "A2.opt",
  Tc1_var = NULL,
  Tt1_var = NULL,
  Tc2_var = NULL,
  Tt2_var = NULL,
  Tc_total_var = NULL,
  Tt_total_var = NULL,
  as.data.frame = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted object of class \code{"Drmatch"}.}

\item{newdata}{A data frame on which to compute predicted treatment
assignments and performance summaries.}

\item{stage}{Character string indicating which decision stage to summarize.
One of \code{"both"}, \code{"stage1"}, or \code{"stage2"}. Default is \code{"both"}.}

\item{obs1_var}{Optional character string giving the observed stage-1
treatment column in \code{newdata}. If \code{NULL}, the value is taken from
\code{object$A1.var}.}

\item{obs2_var}{Optional character string giving the observed stage-2
treatment column in \code{newdata}. If \code{NULL}, the value is taken from
\code{object$A2.var}.}

\item{eta2_var}{Optional character string giving the stage-2 eligibility
column in \code{newdata}. If \code{NULL}, the value is taken from \code{object$eta2.var}.}

\item{pred1_var}{Character string giving the column name in the prediction
output corresponding to the estimated stage-1 treatment. Default is
\code{"A1.opt"}.}

\item{pred2_var}{Character string giving the column name in the prediction
output corresponding to the estimated stage-2 treatment. Default is
\code{"A2.opt"}.}

\item{Tc1_var}{Optional character string giving the stage-1 value column for
treatment option \code{-1}.}

\item{Tt1_var}{Optional character string giving the stage-1 value column for
treatment option \code{1}.}

\item{Tc2_var}{Optional character string giving the stage-2 value column for
treatment option \code{-1}.}

\item{Tt2_var}{Optional character string giving the stage-2 value column for
treatment option \code{1}.}

\item{Tc_total_var}{Optional character string giving the total value column
for treatment option \code{-1}.}

\item{Tt_total_var}{Optional character string giving the total value column
for treatment option \code{1}.}

\item{as.data.frame}{Logical; if \code{TRUE}, the output is returned as a
one-row data frame. Otherwise, a named numeric vector is returned.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=predict.Drmatch]{predict.Drmatch()}}.}
}
\value{
Either a one-row data frame or a named numeric vector containing the
policy performance metrics returned by \code{\link[=policy_summary_metrics]{policy_summary_metrics()}}.
}
\description{
Generates predicted treatment decisions from a fitted \code{Drmatch} object and
summarizes their performance against observed treatment assignments in a new
dataset. Depending on the requested stage, the function evaluates stage-1
decisions, stage-2 decisions, or both jointly.
}
\details{
Internally, predicted treatment assignments are passed to
\code{\link[=policy_summary_metrics]{policy_summary_metrics()}} to compute stage-specific and joint-path
classification metrics, with optional value-based summaries when outcome
columns are supplied.

When \code{stage = "stage1"}, stage-2 predictions are set to \code{NA} before summary
metrics are computed. When \code{stage = "stage2"}, the observed stage-1
treatment from \code{newdata} is used as the stage-1 component of the joint path,
while stage-2 predictions are taken from \code{predict(object, ...)}.

This method requires \code{newdata} because performance is evaluated by comparing
predicted treatment decisions with observed treatments and, optionally, with
value columns provided in the new dataset.
}
\examples{
\dontrun{
# fit <- Drmatch(...)
# summary(fit, newdata = test_dat)
# summary(fit, newdata = test_dat, stage = "stage1")
# summary(fit, newdata = test_dat, stage = "stage2", as.data.frame = FALSE)
}

}
\seealso{
\code{\link[=policy_summary_metrics]{policy_summary_metrics()}}, \code{\link[=print.summary.Drmatch]{print.summary.Drmatch()}},
\code{\link[=predict.Drmatch]{predict.Drmatch()}}
}
